/// doc-fragment-start:api-object
const api = {
  // This is an example of a dummy implementation
  // of an API object, which would contain a codebase
  // that could be explained in further detail in the
  // README next to it.
}
/// doc-fragment-end:api-object

export { api }
